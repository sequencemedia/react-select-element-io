import React from 'react'
import {
  Route
} from 'react-router'

import IndexPage from 'react-select-element-io/client/app/components/index-page'

export default (
  <Route exact path='/' component={IndexPage} />
)
